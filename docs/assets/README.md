# README visuals

The two PNG files in this directory were generated for this repository with
Codex's built-in image generation workflow. They are decorative README assets,
not upstream project assets and do not contain user data.
