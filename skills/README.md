# Standalone skills

Place skills that are useful on their own under this directory:

```text
skills/<skill-name>/
└── SKILL.md
```

If a skill belongs to a plugin, keep it under that plugin's `skills/` directory
instead of maintaining a second copy here.

